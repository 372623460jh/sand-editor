import BLOCKS from './blocks';

export const DEFAULT_NODE = BLOCKS.PARAGRAPH;

export const SLATE_OBJECT_TYPES = {
  DOCUMENT: 'document',
  BLOCK: 'block',
  INLINE: 'inline',
  TEXT: 'text',
  MARK: 'mark',
};
